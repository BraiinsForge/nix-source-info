[package]
name = "nix-source-info"
version = "0.1.0"
edition = "2024"
publish = ["braiins"]

[dependencies]
ver-stub = "0.3"
chrono = "0.4"
serde = { version = "1", features = ["derive"]}
serde_json = "1"